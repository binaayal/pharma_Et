<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds only. A developer's API runs over plain HTTP on the laptop, reached from a
     USB device through `adb reverse tcp:3000 tcp:3000` (localhost) or from the emulator
     (10.0.2.2). Everything else, and every release build, stays HTTPS-only. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
